const chicken = {
    sound: "bok-bok",
    color: "brown"
};

console.log(chicken.sound, chicken.color);     // Dot notation
console.log(chicken["sound"], chicken["color"]);  // Square bracket notation

const numbers = [1, 345, 57, 2, 32];

const newNumbers = numbers;